DTO có nhiệm vụ nhận dữ liệu từ client gửi về, giống với model ở mô hình mvc trong servlet jsp.

client -> DTO -> controller -> service -> repository

client -> DTO -> API -> service -> repository

swagger